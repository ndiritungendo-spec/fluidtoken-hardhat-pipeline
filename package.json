cat > package.json << 'EOF'
{
  "name": "fluidtoken-hardhat-pipeline",
  "version": "1.0.0",
  "description": "Hardhat project for FluidToken",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.27.0",
    "solhint": "^5.0.2",
    "eslint": "^8.57.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
EOF